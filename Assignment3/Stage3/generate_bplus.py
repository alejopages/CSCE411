'''
Stage 3 - generate B+ tree from files, bottom up.

Notes:

    Define variable length fan-out. (input arg)
    Define root tree node.
    Each file is named with 
    Binary tree Heaps can be encoded in lists, the root at
